{
  "name": "@zengin/hook",
  "version": "0.0.1",
  "description": "Claude Code PostToolUse hook that checks every file write against the design system.",
  "license": "MIT",
  "type": "module",
  "bin": { "zengin-hook": "./dist/index.js" },
  "main": "./dist/hook.js",
  "types": "./dist/hook.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@zengin/engine": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9.2",
    "vitest": "^5.0.0"
  }
}
