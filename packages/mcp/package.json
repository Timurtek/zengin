{
  "name": "@zengin/mcp",
  "version": "0.0.1",
  "description": "MCP server exposing the Zengin conformance engine to coding agents.",
  "license": "MIT",
  "type": "module",
  "bin": { "zengin-mcp": "./dist/index.js" },
  "main": "./dist/server.js",
  "types": "./dist/server.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "@zengin/engine": "workspace:*",
    "zod": "^4.6.2"
  },
  "devDependencies": {
    "@types/node": "^22",
    "typescript": "^5.9.2",
    "vitest": "^5.0.0"
  }
}
