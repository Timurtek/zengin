#!/usr/bin/env node
// The unscoped entry point: `npx zenginui create my-app`. The command line itself lives in @zenginui/cli;
// this package exists so the bare name works with npx, since the bare name "zengin" is not available on npm.
import "@zenginui/cli/dist/index.js";
