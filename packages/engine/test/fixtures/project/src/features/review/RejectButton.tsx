import { Button } from "@zengin/ui";

export function RejectButton() {
  return (
    <Button
      variant="ghost-danger"
      size="xl"
      className="rounded-none bg-red-500 px-[13px] mt-4 w-full"
    >
      Reject
    </Button>
  );
}
