---
"@zenginui/registry": patch
"@zenginui/cli": patch
---

The public registry moved to https://zengin.timurtek.com/r, the site's own domain. The old vercel.app address keeps serving the registry for installed copies.
